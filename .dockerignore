# Local WhatsApp API data (sessions + media) never enters an image or the repo.
whatsapp-data/

# Python artifacts
.venv/
.venv-test/
__pycache__/
*.pyc

# Local config (phone number, etc.)
config.json

# WAHA credentials (API key, dashboard/swagger auth)
.env

# signal-cli downloads / logs
bin/
*.log
