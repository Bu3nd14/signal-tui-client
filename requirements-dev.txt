# Dipendenze di sviluppo / CI — Signal TUI Client
# Installare con:  pip install -r requirements-dev.txt

# --- Test runner ---
pytest>=9.1,<10
# Necessario per i test TUI asincroni basati su App.run_test() (headless).
# Vedi PLAN_TUI_TESTING.md: `asyncio_mode = "auto"` in pyproject.toml.
pytest-asyncio>=0.25

# --- Coverage (plugin pytest + libreria) ---
pytest-cov>=7.1,<8
coverage>=7.15,<8

# --- Lint / format ---
# PINNATO: il set di regole di DEFAULT di ruff cambia tra versioni (0.16.1 ha un
# default più ampio delle 0.9.x). La baseline dei 354 errori è stata misurata con
# 0.16.1: la versione va bloccata per non far "muovere" il gate lint.
ruff==0.16.1

# --- Type checking ---
# PINNATO: gli aggiornamenti di basedpyright possono introdurre nuove diagnostiche;
# la versione va bloccata per non far "muovere" il gate di type checking.
basedpyright==1.39.10
