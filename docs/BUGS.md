# Bug Report — Signal TUI Client

> **Stato:** Revisionato il 23/08/2026 — verifica conclusiva manuale del codice e integrazione dei problemi emersi dalla review architetturale (`documentation/review/ARCHITECT_REVIEW.md`). In data odierna aggiunti i bug **#54** (DuplicateIds su `load-more-msg`, emerso dai log runtime) e **#55** (receipt WhatsApp `@lid` senza match `is_mine`, residuo del #39 osservato sul campo). Il 24/08/2026 aggiunto **#56** (perdita permanente dei messaggi Signal durante finestre di disconnessione), emerso da un caso reale e verificato da shell/bytecode. Sempre il 24/08/2026 aggiunto **#57** (quote di media non-immagine su Signal non fedele al body), emerso dalla review del fix #37 sul branch `fix/quote-media-37-v2`. Il 24/08/2026 **#37 RISOLTO** con PR #53 (V2 + piano B, verifica empirica sul filo reale superata). Il 25/08/2026 aggiunti **#58–#64**: follow-up della feature *immagini native inline (kitty graphics protocol)* sul branch `dev-images` (design `documentation/design/DESIGN_NATIVE_IMAGES.md`), emersi dalle review di tester e architetto (P1–P10/B1–B8; P1–P4 e B1–B2 già risolti pre-merge).
> **Ordinamento:** Per impatto sull'utente finale (dal più grave al meno grave).
> **Nota:** I bug #2, #14, #15 (pattern `_save_cache/_prune_cache/_load_cache` su JSON) sono stati **rimossi** — il passaggio a SQLite li ha resi obsoleti. I problemi della review architetturale (P1-1..P1-4, P2-1..P2-9, P3-1..P3-6) sono correlati alle voci esistenti tramite una riga **Correlazione**; quelli non coperti hanno aperto i nuovi bug **#42–#53** (verificati sul codice, riferimenti file:rigma reali).

---

## Panoramica

Indice di tutti i bug tracciati. La colonna **Sezione** rinvia alle tabelle di dettaglio nel resto del documento (Rimossi/Risolt = tabelle storiche a fondo pagina).
Stato: APERTO / RISOLTO / WON'T FIX (non sarà corretto) / WAD (Work As Designed, comportamento deliberato).
La colonna **Correlazione review** riporta la sigla della review architetturale collegata alla voce, oppure `—`.

| # | Titolo sintetico | Sezione | Stato | Correlazione review |
|---|------------------|---------|-------|---------------------|
| #1 | `_classify_attachments` processa solo il primo attachment | Critici | RISOLTO | — |
| #2 | Cache ridondante in `_process_envelope` | Rimossi | WON'T FIX | — |
| #5 | Logica duplicata per `sent` in `_identify_contact_for_envelope` | Medi | RISOLTO | — |
| #6 | Hot loop del polling; retry SSE a pausa fissa | Medi | APERTO | P1-4 |
| #9 | Ricerca emoji senza alias alternativi | Medi | APERTO | — |
| #10 | Picker emoji duplica la ricerca e omette risultati | Minori | APERTO | — |
| #11 | Modale vuota con stdout catimg vuoto | Minori | APERTO | — |
| #14 | Cache ridondante in `on_list_view_selected` | Rimossi | WON'T FIX | — |
| #15 | Cache ridondante in `on_input_submitted` | Rimossi | WON'T FIX | — |
| #16 | Parsing contatti fragile | Minori | RISOLTO | — |
| #18 | Nuovo download invalida gli URL precedenti | Medi | WAD | — |
| #21 | `_mount_window` omette `attachment_path` obbligatorio | Critici | RISOLTO | — |
| #22 | Worker di invio rileggava `selected_contact` | Risolt | RISOLTO | — |
| #23 | Invio ottimistico persistito dopo errore di rete | Risolt | RISOLTO | — |
| #24 | Scadenze typing/mumbling legate all'arrivo di eventi | Medi | APERTO | — |
| #25 | Webhook multi-allegato perdeva gli allegati dopo il primo | Risolt | RISOLTO | — |
| #26 | Download server esposto sulla LAN senza controllo accessi | Alti | APERTO | — |
| #27 | Webhook WhatsApp accetta POST non autenticati | Alti | APERTO | P1-1 |
| #28 | Messaggi da mittenti fuori rubrica scartati | Alti | APERTO | — |
| #29 | Reply Telegram inviate come messaggi normali | Risolt | RISOLTO | — |
| #30 | Reply a messaggio proprio: autore della quote errato | Medi | APERTO | — |
| #31 | Foto inviate da cache allineate come ricevute | Medi | RISOLTO | — |
| #32 | Foto dello storico Telegram non scaricabili | Medi | RISOLTO | — |
| #33 | `UnboundLocalError` su stream SSE vuoto | Minori | APERTO | P1-4 |
| #34 | Seconda bolla pending senza riga DB su invii deduplicati | Minori | APERTO | P2-5 |
| #35 | Read receipt Telegram perso dopo re-link a caldo | Medi | RISOLTO | — |
| #36 | Caption foto mai bolla di testo dedicata | Medi | RISOLTO | — |
| #37 | Quote immagine: invisibile in ingresso, non creabile da TUI | Medi | RISOLTO | — |
| #38 | Lista principale non aggiornata dopo risoluzione `@lid` | Medi | APERTO | — |
| #39 | Bolla WhatsApp resta "grigia" fino a un receipt successivo | Medi | RISOLTO | P2-6 |
| #40 | Indicatore typing assente per Telegram e WhatsApp | Medi | RISOLTO (TG) / WON'T FIX (WA) | P2-3 |
| #41 | Stato "consegnato" mai mostrato per WhatsApp e Telegram | Medi | RISOLTO (WA) / WAD (TG) | P2-6 |
| #42 | Webhook WAHA single-thread, payload illimitato, ack prima della persistenza | Alti | APERTO | P1-1 |
| #43 | Stato mutabile condiviso senza lock; I/O bloccanti sul thread UI | Alti | APERTO | P1-2 |
| #44 | Perdita dati: `_update_message_id` multi-riga + dedup al boot | Alti | APERTO | P1-3 |
| #45 | Race doppio listener SSE; `restart_sse` dead code | Medi | APERTO | P1-4 |
| #46 | `.env` letto solo per 3 chiavi: drift con docker-compose | Medi | APERTO | P2-1 |
| #47 | Coda eventi senza maxsize; set anti-retry senza bound | Medi | APERTO | P2-2 |
| #48 | Interfaccia async cerimoniale; `get_pairing_qr` viola Liskov | Medi | APERTO | P2-4 |
| #49 | Transizione `sent`→`failed` impossibile (rank guard) | Medi | APERTO | P2-6 |
| #50 | Edit su messaggio mai visto: drop o bolla senza marker edited | Medi | APERTO | P2-7 |
| #51 | Igiene SQLite: init doppio, dedup full-scan ×3, prune solo WA | Medi | APERTO | P2-8 |
| #52 | Lifecycle thread: leak Telethon, shutdown assente | Medi | APERTO | P2-9 |
| #53 | Fallback verso `signal_backend`; `register()` non protetta | Minori | APERTO | P3-5 |
| #54 | `DuplicateIds` sul widget `load-more-msg` al remount | Minori | APERTO | — |
| #55 | Receipt WhatsApp senza match `is_mine` residui (`@lid`) | Medi | APERTO | P2-6 |
| #56 | Messaggi Signal persi durante finestre di disconnessione (niente replay/backfill) | Alti | APERTO | — |
| #57 | Quote di media non-immagine (video/audio/doc/sticker) su Signal: testo sintetico sul filo → "Original message not found" | Medi | APERTO | — |
| #58 | Cursore non ripristinato dopo il placement kitty (cosmetico) | Minori | APERTO | — |
| #59 | Focus border clippa il fondo della thumbnail nativa (kitty) | Minori | APERTO | — |
| #60 | Transmit delle thumbnail sul thread UI: possibile micro-blocco su ssh lento | Medi | APERTO | — |
| #61 | Accesso a `widget._protocol` privato (smell) | Minori | APERTO | — |
| #62 | Margini del modal nativo assumono header/footer a 1 riga | Minori | APERTO | — |
| #63 | Guardia tmux/screen conservativa: kitty-in-tmux con passthrough mai nativo | Minori | APERTO | — |
| #64 | Memoria kitty su chat lunghe: nessuna LRU né ri-trasmissione su eviction | Medi | APERTO | — |

---

## 📋 Bug aperti — sintesi

Riepilogo dei soli bug **APERTO** (35 totali: 7 Alti, 17 Medi, 11 Minori). Dettagli e fix nelle sezioni per severità.

| # | Titolo sintetico | Severità | Correlazione review |
|---|------------------|----------|---------------------|
| #26 | Download server esposto sulla LAN senza controllo d'accesso | Alti | — |
| #27 | Webhook WhatsApp accetta POST non autenticati dalla rete | Alti | P1-1 |
| #28 | Signal scarta i messaggi di mittenti non presenti nei contatti | Alti | — |
| #42 | Webhook WAHA single-thread, payload illimitato, ack prima della persistenza | Alti | P1-1 |
| #43 | Stato mutabile condiviso senza lock; I/O bloccanti sul thread UI | Alti | P1-2 |
| #44 | Perdita dati: `_update_message_id` multi-riga + dedup al boot | Alti | P1-3 |
| #56 | Perdita permanente dei messaggi Signal durante le finestre di disconnessione | Alti | — |
| #6 | Hot loop del polling; retry SSE a pausa fissa | Medi | P1-4 |
| #9 | Ricerca emoji senza alias alternativi | Medi | — |
| #24 | Scadenze typing/mumbling legate all'arrivo di eventi | Medi | — |
| #30 | Reply a messaggio proprio: autore della quote errato | Medi | — |
| #38 | Lista principale non aggiornata dopo risoluzione `@lid` | Medi | — |
| #45 | Race del doppio listener SSE; `restart_sse` dead code | Medi | P1-4 |
| #46 | `.env` letto solo per 3 chiavi: drift con docker-compose | Medi | P2-1 |
| #47 | Coda eventi senza maxsize; set anti-retry senza bound | Medi | P2-2 |
| #48 | Interfaccia async cerimoniale; `get_pairing_qr` viola Liskov | Medi | P2-4 |
| #49 | Transizione `sent`→`failed` impossibile (rank guard) | Medi | P2-6 |
| #50 | Edit su messaggio mai visto: drop o bolla senza marker edited | Medi | P2-7 |
| #51 | Igiene SQLite: init doppio, dedup full-scan ×3, prune solo WA | Medi | P2-8 |
| #52 | Lifecycle thread: leak Telethon, shutdown assente | Medi | P2-9 |
| #55 | Receipt WhatsApp senza match `is_mine` residui (`@lid`) | Medi | P2-6 |
| #57 | Quote media non-immagine su Signal non fedele al body | Medi | — |
| #60 | Transmit thumbnail sul thread UI: micro-blocco su ssh lento | Medi | — |
| #64 | Memoria kitty su chat lunghe: nessuna LRU su eviction | Medi | — |
| #10 | Picker emoji duplica la ricerca e omette risultati | Minori | — |
| #11 | Modale vuota con stdout catimg vuoto | Minori | — |
| #33 | `UnboundLocalError` su stream SSE vuoto | Minori | P1-4 |
| #34 | Seconda bolla pending senza riga DB su invii deduplicati | Minori | P2-5 |
| #53 | Fallback verso `signal_backend`; `register()` non protetta | Minori | P3-5 |
| #54 | `DuplicateIds` sul widget `load-more-msg` al remount | Minori | — |
| #58 | Cursore non ripristinato dopo il placement kitty (cosmetico) | Minori | — |
| #59 | Focus border clippa il fondo della thumbnail nativa (kitty) | Minori | — |
| #61 | Accesso a `widget._protocol` privato (smell) | Minori | — |
| #62 | Margini del modal nativo assumono header/footer a 1 riga | Minori | — |
| #63 | Guardia tmux/screen conservativa: kitty-in-tmux mai nativo | Minori | — |

---

## 🔴 Critici (impatto diretto sull'esperienza utente)

| # | Titolo | Riferimenti (file:riga) | Scenario | Impatto | Fix | Stato | Review |
|---|--------|-------------------------|----------|---------|-----|-------|--------|
| #21 | `_mount_window` omette `attachment_path` obbligatorio → TypeError silenzioso | `signal_tui.py:1540` | Riapertura chat o riavvio app: i messaggi immagine vengono montati da cache con `ImageWidget(attachment_id=..., fallback_text=...)`, senza il parametro obbligatorio `attachment_path`. | Nessun placeholder per le immagini caricate da cache (il TypeError viene ingoiato alla chat); solo i messaggi live funzionavano. Non rilevato dai test esistenti: usavano solo `msg_type="text"`. | Aggiunto `attachment_path=None` esplicito nella chiamata; test `test_image_messages_mount_from_cache` in `test_refresh_chat.py` (suite 370/370). | RISOLTO | — |
| #1 | `_classify_attachments` processa solo il primo attachment | `backends/signal.py:350-369` | Messaggio con più attachment (es. immagine + video): il loop faceva `return` al primo match; il `return ("attachment", "📎 File", None)` finale (riga 369) era dead code. | Media allegati persi: attachment multipli invisibili all'utente. | `_classify_attachments` ora accumula tutti gli attachment; `_extract_message_data` restituisce `list[dict]` (un dict per attachment), il testo produce N `ChatEvent` e il chiamante (`_sse_listener`) accoda tutto; +7 test in `test_backends.py` (singolo, multipli image, misti image+video+audio, testo+attachment, solo testo, sentMessage multipli, envelope vuoto; suite 377/377). | RISOLTO | — |

## 🟠 Alti (sicurezza, integrità o perdita di messaggi)

| # | Titolo | Riferimenti (file:riga) | Scenario | Impatto | Fix | Stato | Review |
|---|--------|-------------------------|----------|---------|-----|-------|--------|
| #26 | Download server esposto sulla LAN senza controllo d'accesso | `backend/download.py:61-72, 91-94` | Un host della LAN raggiunge la porta di download: bind su `0.0.0.0`, `SimpleHTTPRequestHandler` espone la directory temporanea senza autenticazione o token. | Download di attachment o enumerazione dei file serviti da chiunque abbia accesso alla rete locale. | Bind su loopback salvo opt-in esplicito; se serve accesso remoto, usare URL con token non prevedibile, disabilitare il directory listing e applicare un TTL. | APERTO | — |
| #27 | Webhook WhatsApp accetta POST non autenticati dalla rete | `backend/webhook.py:39-59, 93-97` | Un host raggiungibile invia un POST artefatto a `/webhook` (listener WAHA in bind su `0.0.0.0`, senza firma, token o altra autenticazione). | Inserimento di falsi messaggi/eventi nella TUI (spoofing). Gli aspetti server single-thread senza timeout, payload illimitato (DoS) e ack 200 prima della persistenza (at-most-once) non sono coperti qui: vedere #42. | Bind su loopback per default e validazione di una firma HMAC o di un token segreto prima di inoltrare il payload. | APERTO | P1-1 |
| #28 | Signal scarta i messaggi di mittenti non presenti nei contatti | `backends/signal.py:366-400, 605-608` | Ricezione da un nuovo numero o da un contatto non ancora sincronizzato: `_identify_contact_for_envelope` restituisce `None` e `envelope_to_event` interrompe l'elaborazione. | Perdita definitiva del messaggio. | Creare/aggiornare un contatto provvisorio dal mittente dell'envelope, oppure conservare il messaggio finché la rubrica non è aggiornata. | APERTO | — |
| #42 | Webhook WAHA single-thread senza timeout, payload illimitato, ack 200 prima della persistenza (at-most-once) | `backend/webhook.py:39-59, 93-103` | Slow-loris su `0.0.0.0:8088`: il `socketserver.TCPServer` puro (non threading, righe 95-97), senza `settimeout` e con `Content-Length` arbitrario (`rfile.read(length)`, righe 43-44), resta bloccato in `read()` e tutta la ricezione WhatsApp si ferma mentre WAHA accoda e ritrasmette. Oppure burst (sync iniziale di gruppo attivo, retry storm di WAHA, flood da host locale): la coda in memoria volatile `_events` senza maxsize cresce senza limiti. | Disponibilità (ricezione morta finché la connessione bloccante non chiude), memoria illimitata, affidabilità at-most-once della pipeline webhook: un crash tra ack 200 e drain della coda perde gli eventi accodati e WAHA non ritrasmette. Autenticazione/spoofing tracciati in #27; coda illimitata in #47. | Passare a `ThreadingTCPServer` con `settimeout(10)` sul socket e tetto a `Content-Length` (es. 1 MB); per l'at-most-once, documentare il trade-off o spostare l'ack dopo l'enqueue con fsync differito a batch; rifiutare payload `fromMe: true` non riconducibili a un invio locale recente (`_seen_message_keys` conosce già gli id emessi). | APERTO | P1-1 |
| #43 | Stato mutabile condiviso tra ≥6 thread senza lock applicativi; I/O bloccanti sul thread UI | `tui/contacts.py:708`; `tui/edit.py:130-134`; `tui/chat_view.py:456, 653` | Le cache (`backend.cache`, `self._cache`, `self.contacts`, `_typing_contacts`, `_unread_counts`) sono lette/scritte da thread UI, poll worker, send/edit/load worker e pool di resync (4 thread, `backends/whatsapp.py:1146`) senza lock applicativo (l'unico lock del sistema, `_DB_LOCK` in `backend/db.py:28`, serializza solo SQLite); alla selezione contatto `mark_read_sync` fa una POST REST sincrona dal thread UI (`backends/whatsapp.py:1270-1276`); l'edit ottimistico chiama `apply_edit` (scrittura SQLite) sul thread UI; `_merge_backend_cache` condivide gli stessi dict tra cache backend e cache UI senza copia (`tui/chat_view.py:653`). | Freeze dell'interfaccia (selezionare un contatto con WAHA lenta blocca la UI per secondi), duplicati sporadici per race check-then-act del dedup (echo webhook + fetch_history nel pool resync superano entrambi il controllo), errori da `list.sort` concorrenti e stati UI/DB divergenti; correttezza affidata al GIL. | Spostare `mark_read_sync` e l'edit ottimistico fuori dal thread UI (`run_worker(thread=True)`); un `threading.RLock` per backend attorno ai punti di mutazione (`ingest_message`/`apply_edit`/`process_receipt`/`_add_cached_message`); copiare i dict in `_merge_backend_cache` (`dict(m)`); a medio termine event loop unico di mutazione (producer enqueuono, un solo consumer applica a cache+DB+UI). | APERTO | P1-2 |
| #44 | Perdita dati: `_update_message_id` aggiorna più righe e il dedup al boot le cancella | `backend/db.py:261-289, 531-579`; `tui/send.py::_retry_failed_message:459-517` | `_update_message_id` matcha per `(protocol, contact_number, text, is_mine)` sulle righe id-less senza finestra temporale né `LIMIT 1` (righe 281-286): tutte le righe id-less con lo stesso testo nella chat ricevono lo stesso `msg_id` (e il timestamp sovrascritto). Righe id-less legittime esistono davvero: il retry di un messaggio fallito riparte senza `persist=` e senza id (`tui/send.py:507-516`). Al prossimo avvio `_dedup_messages_by_id` — dentro `_load_cache`, quindi a ogni boot per ogni backend (`db.py:156-157`) — raggruppa per `(protocol, contact_number, msg_id, text)` e cancella tutte le copie tranne una (righe 553-574). | Perdita permanente e silenziosa di un messaggio legittimo: inviare "ok", fallire (riga id-less), ritentare ("ok" di nuovo, seconda riga id-less), poi l'echo aggancia l'id reale → al riavvio una riga viene cancellata dal DB. Non rilevabile dai test attuali (coprono solo il caso a riga singola). | In `_update_message_id` aggiungere una finestra temporale (`AND timestamp BETWEEN ?-? AND ?+?`) e limitare a una riga (`ORDER BY ABS(timestamp - ?) LIMIT 1` via subquery, come già fa `_update_message_status_by_text` a `db.py:467-470`); rendere `_dedup_messages_by_id` difensivo: non cancellare quando la partizione contiene timestamp distanti oltre la finestra echo (segnale di id assegnato due volte per errore — loggare invece di cancellare). | APERTO | P1-3 |
| #56 | Perdita permanente dei messaggi Signal ricevuti mentre la TUI è disconnessa (niente replay né backfill) | `backends/signal.py:117-181` (`_connect_sync`), `:1004-1088` (`_sse_listener`/`poll_once`); `backend/rpc.py:387-447` (`receive`/`listen_events`); daemon avviato con `--receive-mode on-connection` (`signal.py:130-132`) | La TUI è chiusa o con SSE giù quando arriva un messaggio: il daemon signal-cli scarica l'allegato ma l'envelope è consegnato SOLO ai client SSE connessi in quel momento; alla riconnessione non c'è replay né backfill. Caso reale (23/08/2026, chat Herzkonigin `+421949301028`): dopo "I asked Samis wife ( she is a doctor )" (20:30:12) l'immagine `kVo4vZUnEif3H5j1tXLR.png` (PNG 720×1600) è stata ricevuta da signal-cli alle 20:48:17 (allegato su disco in `~/.local/share/signal-cli/attachments/`) ma **mai salvata nel DB** della TUI — buco globale di 50 minuti (20:30→21:20) in TUTTE le chat (Signal/WhatsApp/Telegram), rientro con burst di "Test" sui 3 protocolli alle ~21:20. | Perdita definitiva e silenziosa di messaggi (testo e media) durante ogni finestra di disconnessione. Cause concorrenti VERIFICATE da shell/bytecode: (1) `receive` RPC → errore "Receive command cannot be used if messages are already being received" (il daemon in `on-connection` ha già una receive session attiva); (2) `subscribeReceive` → "Method not implemented" in questa modalità; (3) `sendSyncRequest` richiede SOLO `GROUPS/CONTACTS/BLOCKED/CONFIGURATION` + keys (`SyncHelper.requestAllSyncData`, verificato nel bytecode di `libsignal-cli-0.14.7.jar`) — **mai message history**, quindi ripeterla N volte non recupera nulla; (4) nessun backfill al boot: `receive()` è chiamato solo dai test (`tests/test_telegram.py:470`, `tests/test_backends.py:127`), il `_drain_pending_receive` è stato rimosso dal revert `9bbd6ba` (10/08/2026, "no kill, no receive, no logging"); (5) `sendSyncRequest` al boot (verificato 24/08 09:55: `SYNC-REQUEST result=ok` + ~70 eventi) NON ha ri-consegnato alcun messaggio mancante — i soli messaggi notturni recuperati erano WhatsApp, tramite `fetch_history` (WAHA), meccanismo che il backend Signal non ha. Correlazioni: #6 (retry SSE a pausa fissa), #33 (`UnboundLocalError` su stream SSE vuoto), #45 (race doppio listener SSE / `restart_sse` dead code) — famiglia affidabilità SSE che allunga/occulta le finestre di perdita; #28 (perdita Signal per mittenti fuori rubrica) — altra via di perdita Signal; #42/#47 (at-most-once WhatsApp) — stesso tema ma su webhook. | Unico fix che garantisce zero perdite: avviare il daemon con `--receive-mode manual` e (a) `receive` RPC al boot come backfill di ciò che è arrivato durante il downtime + (b) real-time via `subscribeReceive` (push, con `unsubscribeReceive`) oppure polling continuo di `receive(timeout=N)`. Alternativa "soft" (riduce le finestre senza eliminarle): hardening SSE (#6/#33/#45) e rilevamento buchi confrontando `attachments/` vs DB. Decisione al 24/08/2026: evento sporadico, NON si interviene ora — riaffrontare con la soluzione `manual` se il problema si ripete. | APERTO | — |

## 🟡 Medi (funzionalità degradate)

| # | Titolo | Riferimenti (file:riga) | Scenario | Impatto | Fix | Stato | Review |
|---|--------|-------------------------|----------|---------|-----|-------|--------|
| #6 | Errori del polling eliminano lo sleep e il retry SSE è a pausa fissa | `tui/polling.py:18-100`; `backends/signal.py:761-782` | Daemon o rete indisponibili per più tentativi consecutivi. | Le eccezioni in `_poll_worker` raggiungono l'`except` esterno prima dello sleep: il ciclo riparte subito (hot loop, log ripetuti); il listener SSE ritenta a pausa fissa (~1 Hz) senza comunicare all'utente lo stato degradato. La race del doppio listener di `restart_sse` (dead code) è tracciata in #45. | Applicare un backoff bounded con reset dopo successo e mostrare una notifica di connessione/ricezione degradata. | APERTO | P1-4 |
| #9 | Ricerca emoji non indicizza gli alias alternativi | `emoji_picker.py:35-54` | Ricercare un alias non canonico (es. `:smile:` quando l'emoji è indicizzata con un altro nome). | Ricerca/autocomplete emoji incompleta: l'indice conserva solo il nome canonico di ogni emoji. | Indicizzare tutti gli alias per emoji, mantenendo un risultato unico quando più alias corrispondono. | APERTO | — |
| #18 | Ogni nuovo download invalida gli URL precedenti | `backend/download.py:102-139, 175-181` | L'utente apre due download in successione o condivide il primo URL: `_clean_download_dir()` elimina tutti i file serviti prima di pubblicarne uno nuovo. | Un URL già consegnato restituisce 404. WAD — comportamento deliberato: gli URL sono temporanei e destinati al solo uso interno della TUI (visualizzazione/apertura del singolo media); la pulizia a ogni download è scelta di sicurezza/privacy (evita accumulo sul disco di attachment potenzialmente sensibili e link validi indefinitivamente). La gestione duratura dei media avviene via flusso dell'app (cache e DB), non via URL temporaneo. | Se il design cambiasse: nomi univoci e retention temporale/per sessione, rimuovendo solo i file scaduti. | WAD | — |
| #24 | Scadenze typing e mumbling dipendono dall'arrivo di eventi | `tui/polling.py:33-65` | Nessun nuovo evento in arrivo mentre typing/mumbling sono attivi: i timeout vengono valutati solo dentro il `for event in events`. | Typing e mumbling non scadono: lo stato resta visibile indefinitamente. | Valutare le scadenze a ogni ciclo di polling o tramite timer separato, anche con batch vuoti. | APERTO | — |
| #30 | Reply Signal a un proprio messaggio usa l'autore della controparte | `tui/send.py:167-173` | Rispondere a un proprio messaggio nella chat Signal: per ogni reply il worker usa `contact.id` come `quote_author`. | Signal riceve l'autore della controparte come autore della citazione di un messaggio dell'utente locale. | Conservare l'autore/is_mine nei dati della reply e usare l'identità dell'account locale per i messaggi propri. | APERTO | — |
| #37 | Quote di un'immagine: non visibile in ingresso e impossibile da creare dalla TUI | `tui/chat_view.py`; `tui/send.py`; backends | Un contatto quota un'immagine da un altro client (la bolla quote risulta vuota/assente) oppure si vuole rispondere quotando una foto dalla TUI: il click su un'immagine la apre (modal `ImageModalScreen`) e il flusso di reply/quote è pensato solo per i messaggi di testo (click sul `MessageWidget`). | Quote media non visualizzate e non creabili. Verifiche: quote in ingresso confermato NON visibile su Signal; da verificare per WhatsApp e Telegram; da capire anche l'interazione di input per la creazione (tasto dedicato o modifica del comportamento del click, che oggi apre l'immagine). | RISOLTO con PR #53 (branch `fix/quote-media-37-v2`, 24/08/2026) in due fasi: **V2** (design `DESIGN_QUOTE_MEDIA_37_V2.md`) — ingresso con segnaposto tipizzato `quote_text` nei 3 backend (`media_quote_placeholder` in `models.py`, priorità caption reale); uscita con `ImageWidget.ReplyRequested` (Alt+click/Alt+R, click/Enter invariati → modal); contratto display-vs-filo (`quote_message` = caption o `""`, mai segnaposto/omesso; retry normalizzato). **Piano B** (design `DESIGN_QUOTE_MEDIA_37_PLANB.md`) — su Signal la verifica empirica ha mostrato che `quoteMessage=""` non basta (immagine assente): si passa a signal-cli anche `quoteAttachments` (`contentType:filename:previewFile`, thumbnail = file locale), con persistenza di `content_type` (colonna + migrazione) e backfill per i media legacy (`migrate_content_type.py`). **Verifica**: test unit 1389 verdi, test live sul filo reale (account "Roberto BMW") E1/E2/E3/E7 Signal + E5 WhatsApp + E6 Telegram verdi; confermato sul device che la quote Signal mostra l'immagine quotata. **Residuo**: E4 (ingresso da client ufficiale) è un test manuale (`LIVE_TESTS=1 LIVE_MANUAL=1 pytest tests/test_live_quote_media.py::test_e4_ingest_manual`), non ancora eseguito. | RISOLTO | — |
| #38 | Lista principale non aggiornata dopo la risoluzione `@lid` in background: WhatsApp non raggruppato fino a riavvio | `backends/whatsapp.py` (`_lid_resolver_run`; `_load_contacts`) | Primo avvio con cache lid fredda (`wa_lid_map.json` vuota): `_load_contacts` costruisce `self.contacts` PRIMA che il resolver in background (`start_lid_resolver`, avviato automaticamente alla connessione) popoli la mappa; `_lid_resolver_run` salva la mappa su disco ma NON ricostruisce `self.contacts` né ri-renderizza la lista. | I contatti `@lid` restano caricati senza phone: la lista li mostra come gruppi single-member non fusi con Signal/Telegram per tutta la sessione (il raggruppamento compare solo al riavvio/riconnessione successivi, quando la mappa persistita viene letta). Nessuna perdita dati; colpisce solo chi non ha mai aperto il picker (Ctrl+S). | Al termine di `_lid_resolver_run` (o su notifica), rieseguire `_load_contacts()` e ri-renderizzare la lista (es. `_render_contact_list`) per i contatti il cui `@lid` è stato risolto; oppure far ripartire la proiezione `_visible_rows()` su `self.contacts` con le extras aggiornate. | APERTO | — |
| #45 | Race del doppio listener SSE: `restart_sse` dead code e guard del loop rivalutata True dopo il restart | `backends/signal.py:1004-1023, 1034` | Riavvio dell'SSE mentre il vecchio listener è dentro un `urlopen` (timeout socket 30 s): `join(timeout=5)` scade con il thread ancora vivo; `_start_sse_listener` riporta `_polling_active=True` e assegna il nuovo thread; il vecchio, risvegliatosi nel frattempo, rivaluta la guardia del `while` (riga 1034, ora True) e riconnette anche lui. | Due connessioni SSE attive: ogni envelope accodato due volte (receipt e typing duplicati verso la UI; i messaggi sono mitigati dal dedup, gli altri eventi no). Sul rovescio della medaglia, `restart_sse` (righe 1016-1023) non ha alcun chiamante (dead code verificato su `tui/`, `backends/` e root): il flusso di re-link non ha modo di rinnovare una connessione SSE morta. | Token di generazione (`self._sse_generation += 1` a ogni restart; il thread cattura la propria generazione ed esce se diversa) al posto dei flag condivisi; decidere consapevolmente se chiamare `restart_sse()` nel flusso di re-link o rimuoverlo. | APERTO | P1-4 |
| #46 | `.env` consultato solo per 3 chiavi: drift silenzioso con docker-compose | `backends/config.py:30-52, 99, 149, 181, 196, 224, 242`; `backend/webhook.py:24` | L'utente cambia `CLIENT_WEBHOOK_PORT=8090` in `.env`: `_load_dotenv()` è usato solo da `get_whatsapp_api_key` (149), `get_telegram_api_id` (224) e `get_telegram_api_hash` (242); le altre chiavi leggono solo `os.environ`/`config.json` (`WHATSAPP_API_PORT` a 99, `CLIENT_WEBHOOK_PORT` a 181 e `backend/webhook.py:24`, `WAHA_WEBHOOK_URL` a 196). Ma `.env` è il canale documentato per docker-compose (`env_file:` in `docker-compose.yml:52`) e compose sostituisce `${CLIENT_WEBHOOK_PORT}` nella `WAHA_WEBHOOK_URL` (`docker-compose.yml:41`; `.env.example:22` dichiara proprio `CLIENT_WEBHOOK_PORT=8088`). | WAHA posta gli eventi sulla 8090 ma il client continua ad ascoltare sulla 8088 → ricezione WhatsApp totalmente ferma senza alcun errore in UI. Stesso drift per `WHATSAPP_API_PORT`: spostandosi WAHA, `_local_waha_reachable()` (`config.py:119-127`) fallisce e il backend WhatsApp non viene registrato, silenziosamente (`whatsapp_enabled()`, `config.py:163-171`). Trappola operativa armata dalla documentazione stessa (`.env.example`). | Unificare la precedenza in `backends/config.py::_get` (`os.environ` → `config.json` → `.env` → default) per tutte le chiavi (il loader esiste già); oppure dichiarare esplicitamente che `.env` serve solo a docker-compose e rimuovere da `.env.example` le chiavi che il client legge. | APERTO | P2-1 |
| #47 | Coda eventi WhatsApp senza maxsize (guardia drop-oldest irraggiungibile) e set anti-retry senza bound | `backends/whatsapp.py:157, 161, 1338-1348` | Burst sulla webhook (sync iniziale di un gruppo attivo, retry storm di WAHA, flood da host locale): `self._events` è una `queue.Queue()` senza `maxsize` (riga 157), quindi la guardia `except queue.Full` di `_enqueue_event` (righe 1338-1348, drop-oldest) è codice morto — con `maxsize=0` `put_nowait` non solleva mai `Full`. Il producer è il thread webhook (rete, potenziale vettore di flood — vedi #42), il consumer è il poll worker a ~1 s che per ogni evento può scrivere su SQLite; `_seen_message_keys` (riga 161) cresce senza bound per tutta la sessione. | Crescita non limitata di memoria e latenza di visualizzazione sotto burst; su sessioni lunghe il set anti-retry è un leak lento. Nessuna perdita diretta ma memoria non vincolata su un path raggiungibile dalla rete; l'aspetto at-most-once dello stesso problema è in #42. | `queue.Queue(maxsize=N)` rendendo viva la guardia drop-oldest (da ripensare comunque: meglio coalescenza typing/receipt e contatore di drop loggato); limitare i set anti-retry con un LRU (es. `OrderedDict` ~5000 chiavi) o azzerarli a ogni connect. | APERTO | P2-2 |
| #48 | Interfaccia async cerimoniale; `get_pairing_qr` viola Liskov | `backends/base.py:188`; `backends/telegram.py:1319`; `backends/whatsapp.py:421` | Un nuovo chiamante (o contributore/agente che legge `base.py`) usa la superficie async di `ChatBackend`/`BackendManager` (`connect_all`, `disconnect_all`, `send_message`, `mark_read`) credendola non bloccante. | Zero call site nella TUI (verificato: il flusso reale usa i `*_sync` da worker thread); le implementazioni "async" eseguono I/O bloccante nel corpo della coroutine (`WhatsAppBackend.send_message` delega al sync: REST fino a 30 s; `connect` può attendere la session-ready fino a 40 s). `get_pairing_qr`: async nella base (`base.py:188`), override sync in Telegram (`telegram.py:1319`, firma incompatibile), async-ma-bloccante in WhatsApp (`whatsapp.py:421`); la UI bypassa il contratto chiamando direttamente `wa._rest.get_pairing_qr()`. Anche `receive()` è inusato (la TUI usa `poll_once`) e `SignalBackend.receive()` muta `_polling_active`. Debito tecnico/doppio modello mentale: nessun malfunzionamento sui path attuali, ma ogni nuovo chiamante della superficie async è una trappola (event loop o chiamante bloccati per decine di secondi). | Decidere e dichiarare: interfaccia sync-first (`*_sync` come contratto primario, wrapper async generati con `asyncio.to_thread`, come già fatto per `edit_message`/`list_address_book` nella base) oppure implementazioni async vere via `to_thread`; allineare `get_pairing_qr` sui tre backend (sync + `to_thread` nel chiamante); deprecare `receive()` o implementarci sopra il poll worker. | APERTO | P2-4 |
| #49 | Transizione `sent`→`failed` impossibile: rank guard ed `expected_statuses=("pending",)` bloccano il ramo fallimento | `backend/db.py:390-393`; `tui/send.py:240-244, 316-319` | L'echo di WAHA segna la bolla come `sent` prima che l'eccezione del send emerga nel worker (o l'invio fallisce dopo un primo ack): entrambi i percorsi di errore chiamano `_transition_outgoing_status(..., "failed", ("pending",))` (`tui/send.py:242-243` e `317-318`) e la riga non è più `pending`. | Il rank guard SQL (`rank(stato_corrente) <= rank(nuovo_stato)` su `pending=0, failed=0, sent=1, delivered=2, read=3`, tabella ricopiata in 7 punti, es. `backend/db.py:390-393`) impedisce qualsiasi transizione verso `failed` (rank 0); anche il fallback by-text (`_update_message_status_by_text`) fallisce per lo stesso doppio vincolo → aggiornamento `False`, DB/cache/widget restano a `sent`: stato visibile menzognero dopo un fallimento reale e messaggio non ritentabile (il retry parte solo da bolle `failed`, `tui/send.py:471`). Sotto-problema NON coperto da #39/#41. | Permettere `sent`→`failed` esplicitamente dal solo percorso di errore dell'app (`expected_statuses=("pending", "sent")` in `_transition_outgoing_status`), come decisione consapevole e non effetto collaterale del rank; centralizzare la tabella dei rank in `models.py` (oggi ricopiata in 7 punti tra SQL CASE e dict) per evitare drift. | APERTO | P2-6 |
| #50 | Edit ricevuto su messaggio mai visto: drop silenzioso (Signal/Telegram) o bolla sintetica già editata senza traccia (WhatsApp); `apply_edit` a doppio uso | `tui/events.py:195-203`; `backends/whatsapp.py:239-271`; `backends/base.py:126-146`; `tui/edit.py:116-145` | Il contatto scrive e subito edita un messaggio mentre la chat non è aperta in TUI: edit riferito a un messaggio che la TUI non ha mai visto (inviato/editato da un altro device prima di qualsiasi fetch). | Signal/Telegram: l'evento `message_edit` trova target ignoto, `apply_edit` ritorna `None` e l'handler scarta l'evento in silenzio (`tui/events.py:195-203`) — il messaggio non comparirà mai finché un fetch storico non lo porta (già editato). WhatsApp: l'ack-edit per id sconosciuto non matcha `_detect_edit` e cade nel ramo else (righe 271+): evento sintetico `message` col testo già modificato, senza indicazione `edited`. Inoltre `apply_edit`, documentato nella base come punto UNICO di mutazione per gli edit ricevuti (`base.py:126-146`), è riusato dall'edit locale ottimistico sul thread UI (`tui/edit.py:116-145`): perdita di contenuto visibile e d'informazione d'edit, contratto ambiguo per chi aggiunge un backend. | Separare `apply_edit` (inbound, com'è documentato) da `apply_local_edit` (ottimistico, stessa logica ma nome/contratto espliciti), con persistenza ottimistica spostata nel worker; per l'edit su messaggio ignoto fare upsert come nuovo messaggio con `edited=True` invece del drop; per WhatsApp, quando l'ack-edit non matcha nulla, emettere l'evento sintetico con marker `edited=True` invece del puro `message`. | APERTO | P2-7 |
| #51 | Igiene SQLite: `_init_db` doppio per operazione, dedup full-scan ×3 a boot, prune solo nel path WhatsApp, cache Signal non filtrata | `backend/db.py:18, 104-142, 156-157, 292-317`; `backends/whatsapp.py:1153`; `backends/signal.py:328-330` | Boot normale e uso quotidiano: ogni funzione di `backend/db.py` chiama `_init_db()` (connessione + `PRAGMA journal_mode=WAL` + verifica migrazione, righe 104-142) e apre poi una seconda connessione per il lavoro vero; `_load_cache` esegue sempre `_dedup_messages_by_id` (full-scan con window function + 2 COUNT, righe 156-157), chiamata una volta per backend al boot (×3); `_prune_cache` (200 messaggi/contatto, righe 292-317) è chiamata solo da `WhatsAppBackend.resync_history` (`backends/whatsapp.py:1153`); `SignalBackend._load_protocol_cache` chiama `_load_cache()` senza filtro di protocollo (`signal.py:330`). | Nessun rischio di corruzione (il lock funziona) ma igiene strutturale degradata: due connessioni e due acquisizioni di `_DB_LOCK` per operazione; performance di boot che scala male con la dimensione del DB; installazioni solo Signal/Telegram non potano mai → storage illimitato (e mai `VACUUM`); `CACHE_RETENTION_DAYS = 3` (`db.py:18`) costante morta con docstring stale (riga 293); cache Signal include righe WhatsApp/Telegram (chiavi raw fantasma nel merge UI di `_on_backend_ready`). | Init una tantum per processo (la migrazione è già gated da `user_version`) e connessione singola per operazione; spostare `_dedup_messages_by_id` fuori da `_load_cache` (una volta per boot, esplicita); chiamare `_prune_cache` anche dal connect Signal/Telegram + `VACUUM` occasionale a prune; filtrare con `_load_cache(protocol="signal")`; eliminare o riattivare `CACHE_RETENTION_DAYS` allineando la docstring. | APERTO | P2-8 |
| #52 | Lifecycle thread: loop Telethon mai fermato (leak a ogni reconnect), server webhook/download senza shutdown, fan-out manager in serie fino a ~75 s | `backends/telegram.py:189-204, 216-228`; `backend/webhook.py:102-103`; `backends/manager.py:97-113`; `tui/app.py:262-269` | Vari Ctrl+L su Telegram in sessione; apertura del picker rubrica con backend appesi; shutdown dell'app. | `TelegramBackend.disconnect_sync` chiama `client.disconnect()` ma non ferma mai il loop (`run_forever` senza `loop.stop()`): il `join(timeout=5)` scade e ogni `(re)connect` crea un nuovo loop/thread (`_connect_sync` richiama `disconnect_sync` a `telegram.py:216` e ricrea a 220-228) → accumulo di thread zombie. Il server webhook (`serve_forever` in thread daemon, `webhook.py:102-103`) non viene mai fermato: `on_exit` (`tui/app.py:262-269`) disconnette solo Telegram — Signal/WhatsApp/webhook chiudono per abbandono del processo. `list_address_book_sync` attende i `future.result(timeout=25)` in serie (`manager.py:102-104`): con tre backend appesi il worker resta bloccato fino a ~75 s. Leak lento di thread, shutdown fragile (qualunque futuro porting embedded/di test eredita thread penzolanti), UI bloccata a lungo sull'apertura della rubrica. | In `disconnect_sync` schedulare `loop.call_soon_threadsafe(loop.stop)` prima del join; valutare i future con `concurrent.futures.wait(..., timeout=25)` complessivo invece che per-future in serie; in `on_exit` chiamare `disconnect_all()` (esiste già, inusato) e lo shutdown del server webhook. | APERTO | P2-9 |
| #39 | Messaggio WhatsApp inviato resta "grigio" (pending) in UI fino a un receipt successivo | `tui/send.py` (`_transition_outgoing_status`); `backends/whatsapp_events.py` (`_ack_value`/`_event_from_ack`) | Inviare un messaggio WhatsApp dalla TUI: la bolla ottimistica resta grigia (CSS `.msg-pending`) finché un receipt successivo (es. il destinatario legge per rispondere) la corregge; il DB raggiunge invece lo stato finale corretto (`delivered`). Due concause: (1) early-return silenzioso di `_transition_outgoing_status` — ritorna `False` senza aggiornare né DB, né cache UI, né widget se la riga DB non è trovata o lo stato non è più `pending`; (2) mappatura ack WAHA divergente dalla docu ufficiale (app: 2=SERVER_ACK/3=DELIVERY/4=READ vs docu: 1=SERVER/2=DEVICE→consegnato/3=READ/4=PLAYED) → receipt di consegna ignorato e read trattato come delivered. | Feedback d'invio errato/ritardato nella sola bolla live; aggiornamenti intermedi di stato sporadici. | Fix su branch `fix/wa-receipt-id-match` (mergeato): Fix B applicato con il #41 (enum ack ufficiale WAHA); root cause del match fallito scoperta sul campo (21/08/2026): WAHA 2026.8.1 WEBJS usa id diversi tra DB/cache (`true_{jid}_{hex}_{participant@lid}`) e payload ack (spesso senza partecipante, hex troncato) → nuova funzione pura `canonical_msg_id()` in `backends/whatsapp_events.py` (validata sui 1124 id reali del DB: 0 errori, 0 collisioni), `_event_from_ack` normalizza l'id del receipt, `process_receipt` confronta per id canonico con fallback per unicità (una sola entry `is_mine` `sent` id-less in chat) e `logger.warning` solo su vero mismatch; strumentazione su early-return `False`. Test `tests/test_whatsapp_receipt_id_match.py` (20 test). Aggiuntivo (21/08/2026, branch `fix/outgoing-status-fallback`): race pending→sent (l'echo WAHA sostituisce il timestamp ottimistico prima della transizione → match per timestamp fallisce) → nuova `_update_message_status_by_text` come fallback in `_transition_outgoing_status`; cache backend/UI aggiornate per testo+is_mine; test `tests/test_outgoing_status_fallback.py` (7 test); suite completa 1181 passed. | RISOLTO | P2-6 |
| #40 | Indicatore di digitazione (typing) non funzionante per Telegram e WhatsApp, solo per Signal | `backends/telegram.py`; `backends/whatsapp.py`/`backends/whatsapp_events.py`; `backends/signal.py:759-764` | L'utente osserva la lista contatti mentre un contatto digita: l'indicatore compare solo per i contatti Signal (unico con pipeline typing completa: `typingMessage` → `ChatEvent(type="typing")`, righe 759-764). Nessun evento `type="typing"` nei log per Telegram/WhatsApp; test esistenti solo su Signal (`test_envelope_to_event_typing`) e sulla normalizzazione unitaria WhatsApp (`test_typing_event`), nessun test end-to-end. | Telegram: nessuna pipeline typing (nessun handler degli update MTProto). WhatsApp: normalizzazione esistente (`_event_from_typing` in `backends/whatsapp_events.py:433-454`) ma eventi presence mai distribuiti a causa della subscribe per-chat rotta (vedi Fix). Nessun segnale in UI che il typing WhatsApp è disattivato. | Telegram — fix (branch `fix/typing-delivered-40-41`, design `DESIGN_FIX_40_41.md`): nuovo traduttore `_handle_typing_update` che converte `UpdateUserTyping`/`UpdateChatUserTyping`/`UpdateChannelUserTyping` in `ChatEvent(type="typing")` (action STARTED/STOPPED; `SendMessageCancelAction` → STOPPED, le altre `SendMessage*Action` → STARTED), stesse convenzioni `contact_id` di `_handle_read_receipt`, dispatch nel ramo `events.Raw` di `_on_raw`; classe `TestTelegramTyping` (8 test) in `tests/test_telegram.py`; suite 1154 passed. WhatsApp — interventi applicati ma inefficaci su WEBJS: (1) `presence.update` aggiunto agli eventi sottoscritti (`_configure_webhook` + `WAHA_WEBHOOK_EVENTS` in `docker-compose.yml`); (2) `_event_from_typing` allineato alla shape ufficiale WAHA (`payload.presences[].lastKnownPresence` + fallback legacy, filtro online/offline/unavailable → `None`); (3) subscribe per-chat best-effort/lazy (`backends/whatsapp_rest.py::presence_subscribe`); test in `tests/test_whatsapp_fix_40_41.py` (25). WON'T FIX (WhatsApp) — limite dell'engine WEBJS di WAHA 2026.8.1 tier CORE (verificato 21/08/2026): l'endpoint `/api/{session}/presence/{chatId}/subscribe` risponde HTTP 500 per ogni JID (`subscribePresence is not a function`: metodo assente dal core WEBJS); senza subscribe WAHA non distribuisce `presence.update` (0 eventi nei log, anche dopo il fix config webhook e con `config.webjs.tagsEventsOn: true`); il supporto completo esiste solo sull'engine NOWEB, che richiede re-link (scan QR) e store abilitato — scelta non adottata. Il codice implementato resta e diventa operativo se WAHA corregge `subscribePresence` o si passa a NOWEB; subscription disabilitata per default (`_PRESENCE_SUBSCRIBE_ENABLED=False`, riabilitabile con env `WAHA_PRESENCE_SUBSCRIBE=1`); follow-up perf (21/08/2026): sweep al connect e lazy subscribe generavano centinaia di richieste 500 inutili (+1 test; suite 1174 passed). | RISOLTO (Telegram) / WON'T FIX (WhatsApp) | P2-3 |
| #41 | Stato "consegnato" mai mostrato per WhatsApp e Telegram: si passa direttamente da "sent" a "letto" | `backends/whatsapp_events.py` `_event_from_ack:377-431`; `backends/telegram.py` `_handle_read_receipt:938-976` | Inviare un messaggio e osservare la bolla: su Signal il flusso sent → delivered → read funziona (`receiptMessage` con `is_delivery`/`is_read`, `backend/rpc.py` `_process_receipt`); su WhatsApp e Telegram si salta lo stato intermedio (o si resta "inviato" se il receipt di lettura non arriva). | WhatsApp: `_event_from_ack` assume l'enum Baileys (2=SERVER, 3=DELIVERY, 4=READ); se la build WAHA emette i valori della docu ufficiale (1=SERVER, 2=DEVICE→consegnato, 3=READ), l'ack di consegna (2) viene scartato (`status < 3`, righe 422-423) — stessa root cause del #39. Telegram: gestito esclusivamente `UpdateReadHistoryOutbox` (solo eventi receipt `is_read=True`); nessun percorso emetteva `delivered`, benché `process_receipt` supporti il target (righe 992-1000). | WhatsApp — fix (branch `fix/typing-delivered-40-41`): enum ack ufficiale WAHA con costanti condivise in `backends/whatsapp_events.py` (`WAHA_ACK_SERVER=1`, `WAHA_ACK_DEVICE=2`, `WAHA_ACK_READ=3`, `WAHA_ACK_PLAYED=4`); `_event_from_ack`: soglia `status >= 2` per produrre il receipt (DEVICE → delivered, `is_read=False`; READ → `is_read=True`), `_ack_value` rimappa i nomi ufficiali (alias Baileys rimossi); soglie `fetch_history` allineate (`>= WAHA_ACK_DEVICE` / `>= WAHA_ACK_READ`); compatibilità ordinamento message/receipt verificata (con ack=2: `[message, receipt(delivered)]` senza duplicati). Test aggiornati (`tests/test_whatsapp_backend.py`, `tests/test_whatsapp_read_receipt_fix.py`, `tests/test_edit_whatsapp.py`) + 25 nuovi in `tests/test_whatsapp_fix_40_41.py`. Telegram: nessun delivered sintetico — protocol limitation (by design): MTProto per cloud chat non espone conferma di consegna nativa, solo lettura (`UpdateReadHistoryOutbox`, già gestita); limitazione documentata nei docstring di `_handle_read_receipt` e `process_receipt`, che mantiene il target `"delivered"` per un eventuale futuro supporto. | RISOLTO (WhatsApp) / WAD (Telegram) | P2-6 |
| #32 | Le foto Telegram dello storico non sono scaricabili né apribili | `backends/telegram.py:411-427, 460-466, 482-500` | Aprire una chat con foto Telegram caricate dalla cronologia: il download avviene solo nel gestore live; lo storico costruisce il placeholder senza path e, diversamente dai documenti, senza un identificatore utile al download lazy/on-demand. | Foto storiche non visualizzabili. | Fix (branch `fix/telegram-history-photos`, design `DESIGN_FIX_32.md`): download lazy on-demand (pattern già usato da WhatsApp) — foto e documenti dello storico (e live con download fallito) persistono `attachment_id` come riferimento strutturato `tgref:<chat_id>:<msg_id>` in `_message_to_chat_event`; `get_attachment_path` risolve in 4 passi (path vuoto → file locale esistente → parse `tgref:` → download lazy sul loop Telethon con `get_input_entity` + `get_messages(ids=)` + `download_media`, nome deterministico `{chat_id}-{msg_id}-{nome}`, dedup su file presente, timeout 30 s, fallimento non bloccante → `None`, la UI usa i fallback); niente download eager in `fetch_recent_history` né migrazione schema. Test: 11 nuovi + 2 aggiornati in `tests/test_telegram.py`, 2 aggiornati in `Telegram/test_telegram_backend.py`; suite 914 passed (unico fallimento pre-esistente non correlato in `test_address_book.py`). Limiti noti: righe legacy con `attachment_id=NULL` o bare `msg.id` restano non apribili (dedup impedisce la riscrittura); il click su una foto storica può bloccare la UI fino a 30 s (stesso comportamento WhatsApp già accettato; follow-up: risoluzione del click in worker thread). | RISOLTO | — |
| #31 | Foto inviate renderizzate da cache allineate come ricevute e senza colore `$success` | `tui/chat_view.py:567-577` | Riaprire una chat e vedere una propria foto inviata (es. WhatsApp) caricata da cache: nel ramo cache `_build_message_widgets` crea l'`ImageWidget` senza assegnare `msg-left`/`msg-right` in base a `is_mine` (a differenza del rendering live `_render_image_in_chat`, righe 230 e 240). | La foto inviata resta allineata a sinistra come una ricevuta e non riceve il colore `$success` che `msg-right` applicherebbe (`tui/css.py:98-102`): attribuzione ingannevole per l'utente. Riguarda qualsiasi protocollo nel ramo cache (Signal, Telegram, WhatsApp). | Fix: `_build_message_widgets` ora assegna `widget.classes = "msg-right" if is_mine else "msg-left"` all'ImageWidget (stessa logica del live); applicato contestualmente al #36 (branch `fix/image-caption-alignment`, design `DESIGN_FIX_31_36.md`). Test `TestCacheImageAlignment` in `tests/test_image_caption.py` (parametrizzato su Signal/WhatsApp/Telegram); suite 903 passed. | RISOLTO | — |
| #36 | La caption delle foto non è mai una bolla di testo dedicata: Signal/WhatsApp la mostrano solo nel placeholder da cache e il live la sovrascrive col filename, Telegram la perde del tutto | `backends/signal.py:476-489`; `backends/whatsapp_events.py:162, 184-186, 205-215, 233-239`; `backends/telegram.py:711, 731-733`; `tui/chat_view.py:117-124, 265-276, 567-577` | Ricevere o inviare una foto con didascalia su Signal, WhatsApp o Telegram. | Signal/WhatsApp: caption catturata in `attachment_info` ma usata solo come etichetta del placeholder (`[immagine <caption>]` dal ramo cache) e sovrascritta dal filename nel live (`_finish_attachment_resolve`, righe 265-276). Telegram: caption presente in `text` (riga 711) ma `attachment_info` hardcodato a "Photo" con emoji (riga 733) con precedenza su `text`: caption mai visibile e placeholder ridondante doppio. La modale `ImageModalScreen` mostra solo l'immagine: didascalia indisponibile come testo dedicato su tutti i protocolli (in modo totale su Telegram, fuori dal percorso live su Signal/WhatsApp). | Fix (branch `fix/image-caption-alignment`, design `DESIGN_FIX_31_36.md`): resolver `_image_caption()` in `tui/chat_view.py` (euristica UI-side senza campi/schema nuovi, distingue caption reali da etichette tecniche: filename, mime, fallback, testi sintetici); caption come bolla `MessageWidget` dedicata sotto l'ImageWidget (stesso allineamento/status/sender), sia live (`_add_message`) sia cache/storico (`_build_message_widgets`), per foto inviate e ricevute su tutti e tre i protocolli; `backends/telegram.py`: `attachment_info = text or "Photo"` (rimosso l'hardcode che sopprimeva la caption); normalizzazione placeholder (niente doppia emoji né caption duplicata nelle bolle); `backends/whatsapp_events.py`: caption considerata anche da `payload.body` (canale ufficiale WAHA per i media) invece di cascare sul mime; `backends/whatsapp.py` (percorso ack): stessa sorgente caption per gli outgoing + fix doppio rendering — `_message_already_cached` confronta l'`id` PRIMA del testo (l'evento sintetico `message.ack`, text=caption, non veniva deduplicato contro il messaggio media reale), stessa precedenza id-su-testo nel mirror UI (`_merge_backend_cache._find_existing`). Test: `tests/test_image_caption.py` (`TestImageCaptionResolver`, `TestCaptionBubbleLive`, `TestCaptionBubbleCache`) + `test_message_photo_with_caption_uses_text_as_info` in `tests/test_telegram.py` + 6 casi ack/caption/dedup in `tests/test_whatsapp_backend.py`; suite 903 passed. Limiti noti (bassa severità): caption utente identica a un'etichetta tecnica resta nel placeholder; se l'ack arrivasse prima dell'echo media (ordine inverso, improbabile: WAHA emette echo prima dell'ack) il dedup per id lascerebbe 1 entry di tipo text. | RISOLTO | — |
| #35 | Read receipt Telegram non riflessa in UI dopo riconnessione/re-link | `backends/telegram.py` (`_reconcile_read_state`); `tui/backend_connect.py` (`_on_backend_ready`) | Il destinatario legge un messaggio mentre la TUI è chiusa, poi l'utente effettua un re-link a caldo (Ctrl+L → QR flow → `_connect_telegram`) con la UI cache già popolata a `sent`. | `_reconcile_read_state` marca `status="read"` in cache e SQLite PRIMA di accodare l'evento receipt: quando la TUI lo processa, `process_receipt` trova il messaggio già `read` e restituisce `[]` → nessun mirror nella UI cache; su riavvio completo è innocuo (cache vuota popolata già riconciliata), ma sul re-link a caldo `_on_backend_ready` saltava i messaggi con id già visto senza aggiornarne lo status: messaggio letto a TUI chiusa mostrato `sent` fino al riavvio completo. | Fix: S2 del fix WhatsApp (PR #18) — `_on_backend_ready` e `_merge_backend_cache` ora aggiornano lo status (rank-guard) delle entry già presenti invece di saltarle. Resta solo l'evento receipt di riconciliazione di `_reconcile_read_state` come codice morto (cosmetico, non funzionale). | RISOLTO | — |
| #5 | `_identify_contact_for_envelope` logica duplicata per `sent` | `backends/signal.py:320-351` | Envelope `sentMessage` senza match sul destinatario. | Il secondo blocco `sent` cercava solo `dest` senza `dest_number`/`dest_uuid`; un envelope `sentMessage` senza match poteva cadere nella ricerca per `source` (che per un envelope `sent` è l'utente locale, non un contatto reale). | Fix: rimosso il secondo blocco `sent` ridondante; aggiunto `return None` esplicito dopo il primo blocco. | RISOLTO | — |
| #55 | Receipt WhatsApp ancora senza match `is_mine` residuo (contatti `@lid`), warning diagnostico del #39 ricorrente | `backends/whatsapp.py:1759-1779` (`process_receipt`) | Ricezione di receipt per contatti `@lid` (es. `279958920364071@lid`, `85328132063407@lid`, `191023082934508@lid`): `process_receipt` non trova alcuna entry `is_mine` né per id canonico né per fallback di unicità → `logger.warning` (#39 lo emette "solo su vero mismatch"). Nei log del 23/08/2026 i warning persistono con `is_mine_with_id=21/118/40` in cache: il canonical match del fix #39 non copre ancora questi casi. | Stato delle bolle WhatsApp inviate può non essere aggiornato (restano `sent`/pending) per i contatti coinvolti; log warning ripetuti (rumore diagnostico). Nessuna perdita di messaggi. | Estendere il match canonico del #39 ai receipt `@lid` (verificare se la normalizzazione id copre anche l'hex troncato con `participant@lid` già canonizzato); oppure, per i receipt non risolvibili, valutare il fallback sul più recente `is_mine` della chat se univoco; aggiungere test con id `@lid` reali (i 20 di `test_whatsapp_receipt_id_match.py` non coprono `@lid`). | APERTO | P2-6 |
| #57 | Quote in uscita di media non-immagine (video/audio/documento/sticker) su Signal non fedele al body reale | `tui/chat_view.py:197, 713` (solo `msg_type == "image"` monta `ImageWidget`); `tui/send.py:272, 282` (regola R2/`quote_attachments` applicata solo alle reply da `ImageWidget`); `backends/signal.py:559-564` (video/audio → `msg_type="attachment"`) | Rispondere dalla TUI a un messaggio media non-immagine (video, audio, documento, sticker) su Signal: il messaggio è renderizzato come `MessageWidget` (non `ImageWidget`), quindi la reply segue il percorso testuale e invia sul filo il testo sintetico di display ("🎬 Video", "Video: file.mp4", "📎 File", …) come `quoteMessage`. | Il destinatario mostra "Original message not found" (stesso problema F2 del #37 ma per media non-immagine). Il fix #37 (design `DESIGN_QUOTE_MEDIA_37_V2.md` + `_PLANB.md`, branch `fix/quote-media-37-v2`, PR #53) copre solo le immagini; l'ingresso con segnaposto tipizzato è invece coperto per tutti i tipi media (3 backend). Emerso dalla review del tester il 24/08/2026, non introdotto dal fix (pre-esistente). | Generalizzare il percorso del piano B (#37) a tutti i media: estendere `quote_attachments` (`contentType:filename:previewFile`) ai `MessageWidget` con `msg_type != "text"` (video/audio/documento/sticker), con lo stesso contratto display-vs-filo (body reale o `""`, mai testo sintetico di display) e la stessa persistenza di `content_type`. Il `content_type` è già catturato per questi tipi in `_classify_attachments` (`backends/signal.py:546`) — manca solo il percorso di reply media non-immagine. | APERTO | — |
| #60 | Transmit delle thumbnail sul thread UI: possibile micro-blocco della UI su ssh lento | `tui/chat_view.py` (`_finish_native_thumbnail`, transmit via `call_from_thread`); Textual `WriterThread.MAX_QUEUED_WRITES=30` | Burst di miniature native (chat da cache con molte immagini, ssh lenta): `transmit()` (chunk `a=t` da ~10-20KB per immagine) gira sul thread UI dentro la coda del WriterThread; con coda piena (30 write) il `queue.put` può bloccare brevemente il thread UI. | Micro-freeze durante il caricamento di chat ricche di immagini su connessioni lente. Accettato come limite noto (P7 della review architetto); da monitorare a vista. | Spostare il `transmit` nel worker thread (`driver.write` è thread-safe; l'ordine transmit→place è già garantito dalla sequenza `call_from_thread`), oppure introdurre una coda propria per i `a=t` con priorità rispetto ai `a=p`. | APERTO | — |
| #64 | Memoria kitty su chat lunghe: nessuna LRU né ri-trasmissione su eviction (fase 5 opzionale del design) | `tui/app.py` (`post_display_hook`, `_chat_native_ids`); `tui/chat_view.py` (risoluzione path con semaforo) | `_load_all_messages` su chat con centinaia di immagini: ogni miniatura trasmessa (`a=t`) occupa memoria sul terminale kitty finché il widget resta montato; sotto la quota di eviction di kitty (~320MB) i dati vengono scartati e, con la decisione "niente placeholder in modalità nativa", il widget resta **vuoto senza ri-trasmissione automatica**. Rilevante solo su chat molto lunghe (la finestra da 20 messaggi e `_clear_chat`→`d=I` al cambio chat mitigano). | Degrado visivo (immagini mancanti senza fallback testuale) solo in scenari estremi; memoria terminale non vincolata su chat enormi. | Fase 5 opzionale del design: LRU `d=I` (`keep_data=False`) per immagini oltre N posizioni fuori viewport + ri-trasmissione/ri-place automatica al rientro se i dati sono stati evictati; cache thumbnail su disco (`XDG_CACHE_HOME`) per evitare ri-decode Pillow. | APERTO | — |

## 🟢 Minori (comportamenti subottimali ma non bloccanti)

| # | Titolo | Riferimenti (file:riga) | Scenario | Impatto | Fix | Stato | Review |
|---|--------|-------------------------|----------|---------|-----|-------|--------|
| #34 | Invii deduplicati possono lasciare una seconda bolla pending senza riga DB | `tui/send.py` | Inviare due volte rapidamente lo stesso testo alla stessa chat entro la finestra di deduplicazione. | Una seconda bolla ottimistica viene creata mentre l'insert SQLite viene deduplicato; la finalizzazione aggiorna UI e cache solo se l'update DB trova una riga → la seconda bolla resta `pending` senza record persistito. Correlazione con P2-5: identità euristica a finestre `(protocol, cache_key, ts, text)`, finalizzazione dipendente dal match DB (stessa fragilità di fondo: finestre per protocollo, id usato solo "quando c'è"). | Rendere la transizione di UI/cache indipendente dall'insert deduplicato, oppure impedire la creazione della seconda bolla e del relativo worker. | APERTO | P2-5 |
| #10 | Il picker emoji duplica la ricerca e omette risultati | `emoji_picker.py:347-374` | Ricerca dal picker emoji. | `on_input_changed` reimplementa la ricerca invece di usare `search_emoji()`: cerca solo le 1.081 emoji delle categorie, contro le 5.225 considerate da `search_emoji()` → alcuni risultati non compaiono nel picker. | Riusare `search_emoji()` o un indice condiviso per ricerca e autocomplete. | APERTO | — |
| #11 | Output stdout vuoto di catimg apre una modale vuota | `ui_components.py` | `catimg` non produce stdout (ad esempio per un file corrotto). | La modale renderizza testo ANSI vuoto senza errore visibile. | Controllare `ansi_output.strip()` e mostrare un messaggio d'errore esplicito quando l'output è vuoto. | APERTO | — |
| #16 | `_parse_contacts_from_output` parsing fragile | `backends/signal.py:177-195` | Nomi contatto con spazi (es. "Mario Rossi"). | Il parsing basato su `line.split()` non gestiva correttamente i nomi composti. | Fix: sostituito `line.split()` con regex `_RE_CONTACT_LINE` con named groups (commit: vedi git log). | RISOLTO | — |
| #33 | Stream SSE Signal senza eventi causa `UnboundLocalError` | `backends/signal.py:761-775` | Lo stream SSE termina senza produrre elementi: dopo il `for envelope in ...`, il listener valuta `if envelope:` su una variabile mai definita. | `UnboundLocalError` mascherato come connection lost; log fuorviante (la causa reale — daemon giù — non appare mai). È uno dei quattro difetti del listener SSE descritti in P1-4. | Rimuovere il controllo finale oppure inizializzare la variabile e strutturare il loop senza dipendere dall'ultimo elemento iterato. La race del doppio listener di `restart_sse` è tracciata in #45, il retry a ~1 Hz senza backoff in #6. | APERTO | P1-4 |
| #53 | Fallback silenziosi verso `signal_backend` e registrazione non protetta nel manager | `tui/send.py:117-119, 393-395`; `backends/manager.py:32-36`; `tui/backend_connect.py:209-259` | Backend WhatsApp non registrato (WAHA spenta dopo l'avvio); doppia registrazione accidentale; avvio con WAHA raggiungibile ma 0 contatti caricati. | Se `manager.get(protocol)` ritorna `None`, sia l'ingest ottimistico (`tui/send.py:117-119`) sia l'aggiornamento di stato delle bolle (`tui/send.py:393-395`) ricadono su `signal_backend`: la cache Signal si inquina con messaggi di altri protocolli (o operazione no-op confusa). `BackendManager.register()` (`manager.py:32-36`) sovrascrive silenziosamente un backend già registrato per lo stesso protocollo (il primo sparisce senza warning). `_poll_wa_contacts` trattiene `_pending_backends` fino a 120 s (`backend_connect.py:218-221`): auto-selezione della chat iniziale bloccata per due minuti. Comportamenti subottimali/non bloccanti ma confusionari per stato e cache. | No-op esplicito con log/status quando manca il backend del protocollo (niente fallback incrociato verso Signal); guardia/warning in `register()` quando il protocollo è già presente; ridurre il deadline di `_poll_wa_contacts` o procedere all'auto-selezione con i backend già pronti. | APERTO | P3-5 |
| #54 | `DuplicateIds` sul widget `load-more-msg` quando il banner è già presente al remount | `tui/chat_view.py:567-572, 725-733` | Seconda esecuzione di `_mount_window` sulla stessa chat (es. doppio evento di riconciliazione/refresh) senza `_clear_chat` intermedia: `_add_load_more_widget(total - 20)` monta un secondo `Button(id="load-more-msg")` mentre il primo è ancora nel `chat_log` → `textual._node_list.DuplicateIds`. Osservato al boot del 23/08/2026 (`Failed to mount load-more widget`). | Il banner "load more" non viene montato nel ciclo incriminato (eccezione ingoiata a livello DEBUG): i messaggi più vecchi restano inaccessibili fino a un refresh successivo. Nessun crash dell'app. | Rendere l'id univoco per mount (es. `load-more-msg-{reload_token}`) oppure rimuovere/aggiornare il widget esistente prima del mount (`query_one("#load-more-msg")` + `remove`/`update`); aggiungere un test che esegua `_mount_window` due volte di fila. | APERTO | — |
| #58 | Cursore non ripristinato dopo il placement kitty (cosmetico) | `tui/images/kitty_renderer.py:103-108` (place); `tui/app.py` (`_sync_native_images`) | Il `\e[row;colH` del placement lascia il cursore sull'ultima immagine finché Textual non ridisegna il frame successivo (echo locale disabilitato → solo visivo). | Nessun impatto funzionale; solo posizione del cursore non deterministica a fine hook. | Al termine di `_sync_native_images`, riscrivere `move_to(self.cursor_position)` per ripristinare la posizione nota di Textual. | APERTO | — |
| #59 | Focus border clippa il fondo della thumbnail nativa (kitty) | `ui_components.py` (`ImageWidget.on_focus`/`on_blur`); `tui/app.py` (`_sync_native_images`, clip a `content_region`) | Con il bordo attivo (focus/selezione), il `content_region` del widget perde 2 righe e il fondo della miniatura viene clippato mentre il widget ha il focus. | Piccolo taglio visivo in basso sulla miniatura a widget focalizzato. Conseguenza accettata del design C2; da verificare a vista. | Se confermato a vista: compensare `y_src`/`h_src` con l'offset del bordo quando `_selected`/focused, oppure emettere il placement sul `region` includendo il bordo. | APERTO | — |
| #61 | Accesso a `widget._protocol` privato (smell) | `tui/chat_view.py:498` (`_resolve_mounted_image_paths`) | Il resolver della finestra da cache legge `widget._protocol` (attributo privato di `ImageWidget`). | Debito tecnico minimo: fragile rispetto a refactor del widget; nessun malfunzionamento attuale. | Esporre una property pubblica (es. `ImageWidget.protocol`) e usarla. | APERTO | — |
| #62 | Margini del modal nativo assumono header/footer a 1 riga | `ui_components.py:790-794` (`_NATIVE_TOP_MARGIN`/`_NATIVE_BOTTOM_MARGIN` = 1) | Il placement hi-res del modal centra l'immagine lasciando 1 riga sopra e 1 sotto: se lo StatusBar/footer passasse a 2 righe (o emergesse una dock bar), l'immagine lo coprirebbe. | Copertura visiva di componenti dock di nuova introduzione; oggi header/bottom-bar sono a 1 riga (ok). | Ricalcolare i margini dal layout reale (altezza di header/footer correnti) invece delle costanti. | APERTO | — |
| #63 | Guardia tmux/screen conservativa: kitty-in-tmux con passthrough mai nativo | `tui/images/detect.py:117` | `if env.get("TMUX") or term.startswith("screen")` → CATIMG incondizionato: un kitty vero dentro tmux ≥3.3 con `allow-passthrough` attivo non avrà mai immagini native. | Comportamento sicuro (nessun falso positivo) ma niente valore nativo in tmux anche quando sarebbe possibile. | Affinare la detection: consentire KITTY in tmux solo se `TMUX` presente e il passthrough è abilitato (query `tmux show -p allow-passthrough`), mantenendo la guardia per screen/tmux vecchio. | APERTO | — |

---

## 🗑️ Bug rimossi (obsolescenza confermata)

| # | Descrizione | Motivo |
|---|-------------|--------|
| #2 | `_process_envelope` salva/ricarica cache ridondantemente | Funzione non esiste più — envelope parsing in `backends/signal.py` usa SQLite |
| #14 | `on_list_view_selected` salva/ricarica cache ridondantemente | Pattern `_save_cache`/`_load_cache` rimosso con SQLite |
| #15 | `on_input_submitted` salva/ricarica cache ridondantemente | Pattern `_save_cache`/`_load_cache` rimosso con SQLite |

## ✅ Bug risolti (storico)

| # | Descrizione | Fix e verifica |
|---|-------------|----------------|
| #22 | Il worker di invio poteva rileggere `selected_contact` e inviare alla chat sbagliata | `protocol` e `contact_id` vengono catturati al submit e passati al worker; aggiunti test di regressione |
| #23 | L'invio ottimistico restava inviato e persistito dopo un errore di rete | Introdotti gli stati `pending`, `sent` e `failed`; DB, cache e UI sono aggiornati atomicamente. Il retry conserva destinatario e contenuto e riusa la riga esistente senza duplicarla. Fix validata. |
| #25 | Webhook WhatsApp multi-allegati perdeva tutti gli elementi dopo il primo | Dedup webhook effimero per `(contact, parent msg_id, testo sintetico)`: gli attachment dello stesso messaggio restano distinti. `parent msg_id`, DB e schema restano invariati; nessuna migrazione. Fix validata. |
| #29 | Le reply Telegram erano visualizzate ma inviate come messaggi normali | ID Telegram originale propagato e persistito, passato a Telethon come `reply_to`; reply senza ID bloccata esplicitamente senza fallback normale e supporto retry sicuro. |
